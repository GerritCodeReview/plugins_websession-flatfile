[access]
	inheritFrom = Public-Plugins
[access "refs/*"]
	owner = group plugins-websession-flatfile
[project]
	description = Replaces the builtin Gerrit WebSession implementation with one that uses a flat file based cache.
